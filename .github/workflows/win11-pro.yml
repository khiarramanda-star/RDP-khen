name: KHEN RDP - Cloudflare
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Pilih versi Windows'
        required: true
        default: 'windows-latest'
        type: choice
        options:
          - windows-latest
          - windows-2022
          - windows-2019
          - windows-2016
      save_backup:
        description: 'Save backup ke artifact?'
        required: true
        default: 'yes'
        type: choice
        options:
          - yes
          - no
  schedule:
    - cron: '0 */5 * * *'

permissions:
  actions: write
  contents: read

env:
  RDP_USER: KhenzOfficial
  RDP_PASS: KhenzDev#12
  RDP_DOMAIN: rdp.xzen.my.id
  DATA_PATH: C:\khen-data
  SAVE_BACKUP: ${{ github.event.inputs.save_backup || 'yes' }}

jobs:
  rdp:
    runs-on: ${{ github.event.inputs.version || 'windows-latest' }}
    timeout-minutes: 350
    
    steps:
    - name: Cleanup
      run: |
        Remove-Item -Path C:\Users\runneradmin\AppData\Local\Temp\* -Recurse -Force -ErrorAction SilentlyContinue
        Remove-Item -Path "C:\Program Files\Microsoft Visual Studio" -Recurse -Force -ErrorAction SilentlyContinue
        Remove-Item -Path "C:\Program Files (x86)\Windows Kits" -Recurse -Force -ErrorAction SilentlyContinue
        Remove-Item -Path C:\Users\runneradmin\.nuget -Recurse -Force -ErrorAction SilentlyContinue
        Write-Host "✅ Cleanup done"
    
    - name: Restore Cache
      uses: actions/cache@v4
      id: cache
      with:
        path: ${{ env.DATA_PATH }}
        key: khen-data-${{ github.run_id }}
        restore-keys: khen-data-
    
    - name: Restore Artifact
      if: env.SAVE_BACKUP == 'yes'
      uses: actions/download-artifact@v4
      if: steps.cache.outputs.cache-hit != 'true'
      with:
        name: khen-data-backup
        path: ${{ env.DATA_PATH }}
      continue-on-error: true
    
    - name: System Info
      run: |
        Write-Host "========================================"
        Write-Host "  SYSTEM INFO - ${{ github.event.inputs.version || 'schedule' }}"
        Write-Host "========================================"
        Write-Host "Backup mode: ${{ env.SAVE_BACKUP }}"
        Write-Host "========================================"
        Get-WmiObject -Class Win32_OperatingSystem
        Get-WmiObject -Class Win32_Processor
        Get-WmiObject -Class Win32_PhysicalMemory | Measure-Object -Property Capacity -Sum
        Write-Host "========================================"
    
    - name: Setup RDP
      run: |
        Write-Host "========================================"
        Write-Host "  SETUP RDP"
        Write-Host "========================================"
        
        reg add "HKLM\SYSTEM\CurrentControlSet\Control\Terminal Server" /v fDenyTSConnections /t REG_DWORD /d 0 /f
        reg add "HKLM\SYSTEM\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" /v UserAuthentication /t REG_DWORD /d 0 /f
        netsh advfirewall firewall set rule group="remote desktop" new enable=Yes
        netsh advfirewall firewall add rule name="RDP" dir=in action=allow protocol=TCP localport=3389
        
        net user ${{ env.RDP_USER }} ${{ env.RDP_PASS }} /add /expires:never /passwordchg:no
        net localgroup Administrators ${{ env.RDP_USER }} /add
        net localgroup "Remote Desktop Users" ${{ env.RDP_USER }} /add
        
        New-Item -ItemType Directory -Force -Path ${{ env.DATA_PATH }}
        New-Item -ItemType Directory -Force -Path ${{ env.DATA_PATH }}\Desktop
        New-Item -ItemType Directory -Force -Path ${{ env.DATA_PATH }}\Documents
        New-Item -ItemType Directory -Force -Path ${{ env.DATA_PATH }}\Downloads
        New-Item -ItemType Directory -Force -Path ${{ env.DATA_PATH }}\Pictures
        New-Item -ItemType Directory -Force -Path ${{ env.DATA_PATH }}\Music
        New-Item -ItemType Directory -Force -Path ${{ env.DATA_PATH }}\Videos
        
        $userPath = "C:\Users\${{ env.RDP_USER }}"
        if (-not (Test-Path $userPath)) {
          New-Item -ItemType Directory -Force -Path $userPath
        }
        
        $folders = @('Desktop','Documents','Downloads','Pictures','Music','Videos')
        foreach ($folder in $folders) {
          $target = "${{ env.DATA_PATH }}\$folder"
          if (Test-Path "$userPath\$folder") {
            Remove-Item -Path "$userPath\$folder" -Force -ErrorAction SilentlyContinue
          }
          cmd /c mklink /J "$userPath\$folder" "$target"
        }
        
        $config = @"
        ╔═══════════════════════════════════════════╗
        ║       KHEN RDP - CLOUDFLARE              ║
        ╠═══════════════════════════════════════════╣
        ║ HOST   : ${{ env.RDP_DOMAIN }}           ║
        ║ USER   : ${{ env.RDP_USER }}             ║
        ║ PASS   : ${{ env.RDP_PASS }}             ║
        ║ DATA   : ${{ env.DATA_PATH }}            ║
        ║ STATUS : ONLINE                          ║
        ╚═══════════════════════════════════════════╝
        "@
        $config | Out-File -FilePath "${{ env.DATA_PATH }}\KHEN-CONFIG.txt" -Force
        
        Write-Host ""
        Write-Host "========================================"
        Write-Host "  ✅ RDP ACTIVE"
        Write-Host "========================================"
        Get-Content "${{ env.DATA_PATH }}\KHEN-CONFIG.txt"
        Write-Host "========================================"
        Write-Host "✅ DATA AMAN! Tidak hilang walau restart"
        Write-Host "========================================"
    
    - name: Cloudflare Tunnel
      env:
        CF_TOKEN: ${{ secrets.CLOUDFLARE_TUNNEL_TOKEN }}
      run: |
        Write-Host "========================================"
        Write-Host "  CLOUDFLARE TUNNEL"
        Write-Host "========================================"
        
        if (-not $env:CF_TOKEN) {
          Write-Host "❌ ERROR: CLOUDFLARE_TUNNEL_TOKEN not set!"
          Write-Host "⚠️  Tambah secret di Settings → Secrets → Actions"
          Write-Host "🔑 Token format: cfut_..."
          exit 1
        }
        
        Write-Host "✅ Token ditemukan (length: $($env:CF_TOKEN.Length))"
        
        curl -s -L -o cloudflared.exe https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe
        
        Write-Host "🔄 Starting Cloudflare Tunnel..."
        Start-Process -FilePath ".\cloudflared.exe" -ArgumentList "tunnel --url tcp://localhost:3389 --hostname ${{ env.RDP_DOMAIN }} --token $env:CF_TOKEN" -WindowStyle Hidden -PassThru
        
        Start-Sleep -Seconds 10
        
        $running = Get-Process -Name "cloudflared" -ErrorAction SilentlyContinue
        if ($running) {
          Write-Host ""
          Write-Host "========================================"
          Write-Host "  ✅ CLOUDFLARE TUNNEL ACTIVE"
          Write-Host "========================================"
          Write-Host "HOST   : ${{ env.RDP_DOMAIN }}"
          Write-Host "USER   : ${{ env.RDP_USER }}"
          Write-Host "PASS   : ${{ env.RDP_PASS }}"
          Write-Host "========================================"
          Write-Host "⚠️  Tunggu 1-2 menit, DNS propagate"
          Write-Host "========================================"
        } else {
          Write-Host "❌ Cloudflare Tunnel gagal start!"
          Write-Host "⚠️  Cek token di dashboard Cloudflare"
          exit 1
        }
    
    - name: Keep Alive & Backup (Conditional)
      run: |
        Write-Host "========================================"
        Write-Host "  KEEP ALIVE & BACKUP"
        Write-Host "========================================"
        Write-Host "Save backup mode: ${{ env.SAVE_BACKUP }}"
        Write-Host "========================================"
        
        $counter = 0
        while ($true) {
          $counter++
          Write-Host "$(Get-Date) - KHEN RDP ACTIVE #$counter"
          
          # Backup setiap 5 menit (hanya jika SAVE_BACKUP = yes)
          if (${{ env.SAVE_BACKUP }} -eq 'yes' -and ($counter % 1 -eq 0)) {
            Write-Host "📤 Creating backup..."
            $zipPath = "C:\khen-backup-$counter.zip"
            Compress-Archive -Path ${{ env.DATA_PATH }}\* -DestinationPath $zipPath -Force -ErrorAction SilentlyContinue
            Write-Host "✅ Backup created: $zipPath"
          } elseif (${{ env.SAVE_BACKUP }} -eq 'no') {
            # Skip backup, hanya tampilkan status
            if ($counter % 6 -eq 0) {
              Write-Host "ℹ️ Backup disabled (SAVE_BACKUP=no)"
            }
          }
          
          Start-Sleep -Seconds 300
        }
    
    - name: Upload Backup (Conditional)
      if: env.SAVE_BACKUP == 'yes'
      uses: actions/upload-artifact@v4
      with:
        name: khen-data-backup
        path: ${{ env.DATA_PATH }}
        retention-days: 90
    
    - name: Upload Backup (Final) - Disabled
      if: env.SAVE_BACKUP == 'no'
      run: |
        Write-Host "========================================"
        Write-Host "  SKIP UPLOAD BACKUP"
        Write-Host "========================================"
        Write-Host "ℹ️ SAVE_BACKUP = no, backup tidak di-upload"
        Write-Host "========================================"
    
    - name: Trigger Next Run
      if: always()
      run: |
        curl -s -X POST \
          -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/${{ github.repository }}/actions/workflows/win11-pro.yml/dispatches \
          -d '{"ref":"main"}'
      continue-on-error: true
