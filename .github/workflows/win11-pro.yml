name: KHEN WINDOWS 11 GAMING ULTIMATE

on:
  workflow_dispatch:
    inputs:
      colab_ip:
        description: 'IP Tailscale Colab (contoh: 100.x.x.x)'
        required: true
        default: '100.114.92.84'
      windows_version:
        description: 'Pilih versi Windows'
        required: true
        default: 'windows-11-gaming'
        type: choice
        options:
          - windows-11-gaming
          - windows-11-pro
          - windows-10-pro
      save_data:
        description: 'Simpan data setelah session?'
        required: true
        default: 'yes'
        type: choice
        options:
          - 'yes'
          - 'no'
      duration:
        description: 'Durasi session (jam)'
        required: true
        default: '5'
        type: choice
        options:
          - '1'
          - '2'
          - '3'
          - '4'
          - '5'

permissions:
  actions: write
  contents: read

env:
  RDP_USER: KhenzOfficial
  RDP_PASS: KhenzDev#12
  DATA_PATH: C:\khen-data
  GAME_PATH: D:\games
  BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
  CHAT_ID: ${{ secrets.CHAT_ID }}

jobs:
  rdp-gaming:
    runs-on: windows-latest
    timeout-minutes: 350
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cleanup Space
        shell: pwsh
        run: |
          Write-Host "========================================"
          Write-Host "  KHEN RDP ULTIMATE WINDOWS 11"
          Write-Host "========================================"
          Write-Host "Windows Target: ${{ github.event.inputs.windows_version }}"
          Write-Host "Target IP Colab: ${{ github.event.inputs.colab_ip }}"
          Write-Host "Durasi Session: ${{ github.event.inputs.duration }} jam"
          Write-Host "========================================"
          
          $tempPaths = @(
            "C:\Users\runneradmin\AppData\Local\Temp\*",
            "C:\Program Files\dotnet"
          )
          
          foreach ($path in $tempPaths) {
            if (Test-Path $path) {
              Remove-Item -Path $path -Recurse -Force -ErrorAction SilentlyContinue
            }
          }

      - name: Restore Cache Data
        id: cache
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.DATA_PATH }}
            ${{ env.GAME_PATH }}
          key: khen-data-${{ github.run_id }}
          restore-keys: |
            khen-data-

      - name: Setup RDP Windows
        shell: pwsh
        run: |
          $rdpPort = 3389
          "RDP_PORT=$rdpPort" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          
          Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0 -Type DWord -Force
          Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "UserAuthentication" -Value 0 -Type DWord -Force
          
          Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "fDisableWallpaper" -Value 1 -Type DWord -Force
          Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "fUseUDP" -Value 1 -Type DWord -Force
          
          netsh advfirewall firewall set rule group="remote desktop" new enable=Yes
          
          net user ${{ env.RDP_USER }} ${{ env.RDP_PASS }} /add /expires:never /passwordchg:no
          net localgroup Administrators ${{ env.RDP_USER }} /add
          net localgroup "Remote Desktop Users" ${{ env.RDP_USER }} /add
          
          New-Item -ItemType Directory -Force -Path ${{ env.DATA_PATH }} -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Force -Path ${{ env.GAME_PATH }} -ErrorAction SilentlyContinue

      - name: Optimasi Performa CPU dan Gaming
        shell: pwsh
        run: |
          Write-Host "Mengaktifkan Power Plan High Performance..."
          powercfg -setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c 2>$null
          
          Write-Host "Mengaktifkan DirectX WARP & Graphics Acceleration..."
          reg add "HKLM\SOFTWARE\Microsoft\DirectX" /v D3D12EnableExperimentalFeatures /t REG_DWORD /d 1 /f 2>$null
          reg add "HKLM\SYSTEM\CurrentControlSet\Control\GraphicsDrivers" /v D3D12WarpDevice /t REG_DWORD /d 1 /f 2>$null
          reg add "HKCU\Software\Microsoft\GameBar" /v AllowAutoGameMode /t REG_DWORD /d 1 /f 2>$null
          reg add "HKCU\Software\Microsoft\GameBar" /v AutoGameModeEnabled /t REG_DWORD /d 1 /f 2>$null

      - name: Install Game Runtimes
        shell: pwsh
        run: |
          Set-ExecutionPolicy Bypass -Scope Process -Force
          [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
          
          Write-Host "Installing Direct X and Visual C++ Redistributable..."
          Invoke-Expression ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1')) 2>$null
          choco install directx vcredist-all -y --limit-output --ignore-checksums 2>$null

      - name: Install & Connect Tailscale
        shell: pwsh
        env:
          TS_AUTHKEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          if (-not $env:TS_AUTHKEY) {
            Write-Host "ERROR: TAILSCALE_AUTH_KEY belum di-set di GitHub Secrets!"
            exit 1
          }
          
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $installer = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $url -OutFile $installer -UseBasicParsing
          Start-Process msiexec.exe -ArgumentList "/i `"$installer`" /quiet /norestart" -Wait
          Start-Sleep -Seconds 5
          
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=$env:TS_AUTHKEY --accept-routes
          Start-Sleep -Seconds 5
          
          $ip = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          "TAILSCALE_IP=$ip" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          Write-Host "Tailscale Runner IP: $ip"

      - name: Setup Port Forwarding ke Colab
        shell: pwsh
        env:
          COLAB_IP: ${{ github.event.inputs.colab_ip }}
        run: |
          Write-Host "Menghubungkan jaringan Windows Runner ke IP Colab GPU: $env:COLAB_IP"
          netsh interface portproxy add v4tov4 listenport=3389 listenaddress=0.0.0.0 connectport=3389 connectaddress=$env:COLAB_IP

      - name: Kirim Notifikasi Telegram
        shell: pwsh
        env:
          BOT_TOKEN: ${{ env.BOT_TOKEN }}
          CHAT_ID: ${{ env.CHAT_ID }}
          RDP_PORT: ${{ env.RDP_PORT }}
          TAILSCALE_IP: ${{ env.TAILSCALE_IP }}
          COLAB_IP: ${{ github.event.inputs.colab_ip }}
        run: |
          if ($env:BOT_TOKEN -and $env:CHAT_ID) {
            $message = "RDP WINDOWS 11 GAMING ACTIVE`n`nIP RDP Runner: $env:TAILSCALE_IP`nColab Target IP: $env:COLAB_IP`nPort: $env:RDP_PORT`nUser: ${{ env.RDP_USER }}`nPass: ${{ env.RDP_PASS }}"
            $url = "https://api.telegram.org/bot$env:BOT_TOKEN/sendMessage"
            $body = @{chat_id = $env:CHAT_ID; text = $message} | ConvertTo-Json
            Invoke-RestMethod -Uri $url -Method Post -Body $body -ContentType "application/json"
          }

      - name: Keep Alive Loop
        shell: pwsh
        run: |
          $duration = [int]${{ github.event.inputs.duration }}
          $totalSeconds = $duration * 3600
          $elapsed = 0
          
          while ($elapsed -lt $totalSeconds) {
            $remaining = $totalSeconds - $elapsed
            $hours = [math]::Floor($remaining / 3600)
            $minutes = [math]::Floor(($remaining % 3600) / 60)
            Write-Host "[$(Get-Date -Format 'HH:mm:ss')] $hours jam $minutes menit tersisa"
            Start-Sleep -Seconds 60
            $elapsed += 60
          }

      - name: Upload Backup Data
        if: always() && github.event.inputs.save_data == 'yes'
        uses: actions/upload-artifact@v4
        with:
          name: khen-data-backup
          path: |
            ${{ env.DATA_PATH }}
            ${{ env.GAME_PATH }}
          retention-days: 7
